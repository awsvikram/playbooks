This playbook scans the hosts for missing / faulty lun paths and tries to correct by scanning it .
If it still sees missing HBA, failed paths, missing paths, it stores the server name in a file and mails it to the sys admin
replace the servers in the inventory file with your server names
